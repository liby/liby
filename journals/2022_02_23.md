- 今天遇到了一个 Error payload 多了一级导致抛错提示条没有显示 Error message 的问题
	- 代码中显示报错详情的代码一直在用的 `error.payload.detail`，结果现在发现要使用 `error.data.payload.detail`，多了一级
	- 通过 debug 发现是我们 fork 了一个名为 [mobx-rest-axios-adapter](https://github.com/IranTIP/mobx-rest-axios-adapter) 的库导致的
	- 同时也是最后一次[提交](https://github.com/IranTIP/mobx-rest-axios-adapter/commit/21173776a38f415f5d5d17792ada9cc7d29e63cc)导致出现了 bug
		- ~~`return reject(error.response.data)`~~ => `return reject(error.response)`